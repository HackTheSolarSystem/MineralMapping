# use this file to define standards that are not labeled by thier
# stoichiometric mineral names or do not have a simple chemical formula.
# In this case, give the name of the standard (The same name as the
# `<standard>_mask.tif` file) and a list of elements in that standard with
# the proportion of the element in that standard.

apatite:
    Ca: .3927
    Na: .18
    P: .1777

SCOlv:
    Si: .1908
    Fe: .0742
    Mg: .298
    Mn: .0011
    Ca: .0002
    Ni: .0029
